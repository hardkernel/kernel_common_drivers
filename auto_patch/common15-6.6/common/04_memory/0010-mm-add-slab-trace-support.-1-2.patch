From d563f9ebe6ec8e01219c841000806bfffa44fd0d Mon Sep 17 00:00:00 2001
From: Jianxiong Pan <jianxiong.pan@amlogic.com>
Date: Thu, 21 Dec 2023 17:05:43 +0800
Subject: [PATCH] mm: add slab trace support. [1/2]

PD#SWPL-150034

Problem:
no slab trace.

Solution:
add slab trace.

Verify:
local.

Change-Id: I91f980cf11397f34b29698d7274da1abeb30906c
Signed-off-by: Jianxiong Pan <jianxiong.pan@amlogic.com>
---
 mm/slub.c | 41 +++++++++++++++++++++++++++++++++++++++++
 1 file changed, 41 insertions(+)

diff --git a/mm/slub.c b/mm/slub.c
index 196d3740a2e6..cde130d292c8 100644
--- a/mm/slub.c
+++ b/mm/slub.c
@@ -51,6 +51,9 @@
 #ifdef CONFIG_AMLOGIC_MEMORY_EXTEND
 #include <linux/amlogic/memory.h>
 #endif
+#ifdef CONFIG_AMLOGIC_SLAB_TRACE
+#include <linux/amlogic/slab_trace.h>
+#endif
 /*
  * Lock order:
  *   1. slab_mutex (Global Mutex)
@@ -2052,6 +2055,9 @@ static struct slab *allocate_slab(struct kmem_cache *s, gfp_t flags, int node)
 			return NULL;
 		stat(s, ORDER_FALLBACK);
 	}
+#ifdef CONFIG_AMLOGIC_SLAB_TRACE
+	slab_trace_add_page(slab_folio(slab), oo_order(oo), s, alloc_gfp);
+#endif
 
 	slab->objects = oo_objects(oo);
 	slab->inuse = 0;
@@ -2109,6 +2115,9 @@ static void __free_slab(struct kmem_cache *s, struct slab *slab)
 	__folio_clear_slab(folio);
 	mm_account_reclaimed_pages(pages);
 	unaccount_slab(slab, order, s);
+#ifdef CONFIG_AMLOGIC_SLAB_TRACE
+	slab_trace_remove_page(&folio->page, order, s);
+#endif
 	__free_pages(&folio->page, order);
 }
 
@@ -3198,6 +3207,9 @@ static void *___slab_alloc(struct kmem_cache *s, gfp_t gfpflags, int node,
 	c->freelist = get_freepointer(s, freelist);
 	c->tid = next_tid(c->tid);
 	local_unlock_irqrestore(&s->cpu_slab->lock, flags);
+#ifdef CONFIG_AMLOGIC_SLAB_TRACE
+	slab_trace_mark_object(freelist, addr, s);
+#endif
 	return freelist;
 
 deactivate_slab:
@@ -3263,6 +3275,9 @@ static void *___slab_alloc(struct kmem_cache *s, gfp_t gfpflags, int node,
 
 		if (s->flags & SLAB_STORE_USER)
 			set_track(s, freelist, TRACK_ALLOC, addr);
+	#ifdef CONFIG_AMLOGIC_SLAB_TRACE
+		slab_trace_mark_object(freelist, addr, s);
+	#endif
 
 		return freelist;
 	}
@@ -3289,6 +3304,9 @@ static void *___slab_alloc(struct kmem_cache *s, gfp_t gfpflags, int node,
 		if (s->flags & SLAB_STORE_USER)
 			set_track(s, freelist, TRACK_ALLOC, addr);
 
+	#ifdef CONFIG_AMLOGIC_SLAB_TRACE
+		slab_trace_mark_object(freelist, addr, s);
+	#endif
 		return freelist;
 	}
 
@@ -3298,6 +3316,9 @@ static void *___slab_alloc(struct kmem_cache *s, gfp_t gfpflags, int node,
 		 * we don't make further mismatched allocations easier.
 		 */
 		deactivate_slab(s, slab, get_freepointer(s, freelist));
+	#ifdef CONFIG_AMLOGIC_SLAB_TRACE
+		slab_trace_mark_object(freelist, addr, s);
+	#endif
 		return freelist;
 	}
 
@@ -3421,6 +3442,9 @@ static __always_inline void *__slab_alloc_node(struct kmem_cache *s,
 		}
 		prefetch_freepointer(s, next_object);
 		stat(s, ALLOC_FASTPATH);
+	#ifdef CONFIG_AMLOGIC_SLAB_TRACE
+		slab_trace_mark_object(object, addr, s);
+	#endif
 	}
 
 	return object;
@@ -3778,6 +3802,10 @@ static __always_inline void do_slab_free(struct kmem_cache *s,
 	/* Same with comment on barrier() in slab_alloc_node() */
 	barrier();
 
+#ifdef CONFIG_AMLOGIC_SLAB_TRACE
+	slab_trace_remove_object(head, s);
+#endif
+
 	if (unlikely(slab != c->slab)) {
 		__slab_free(s, slab, head, tail_obj, cnt, addr);
 		return;
@@ -3891,6 +3919,9 @@ int build_detached_freelist(struct kmem_cache *s, size_t size,
 	if (!s) {
 		/* Handle kalloc'ed objects */
 		if (unlikely(!folio_test_slab(folio))) {
+		#ifdef CONFIG_AMLOGIC_SLAB_TRACE
+			slab_trace_remove_page(&folio->page, folio_order(folio), s);
+		#endif
 		#ifdef CONFIG_AMLOGIC_MEMORY_EXTEND
 			if (!aml_free_nonslab_page(folio, object))
 				__free_pages(folio_page(folio, 0), folio_order(folio));
@@ -4540,6 +4571,13 @@ static int calculate_sizes(struct kmem_cache *s)
 	return !!oo_objects(s->oo);
 }
 
+#ifdef CONFIG_AMLOGIC_SLAB_TRACE
+int get_cache_max_order(struct kmem_cache *s)
+{
+	return oo_order(s->oo);
+}
+#endif
+
 static int kmem_cache_open(struct kmem_cache *s, slab_flags_t flags)
 {
 	s->flags = kmem_cache_flags(s->size, flags, s->name);
@@ -5095,6 +5133,9 @@ void __init kmem_cache_init(void)
 	/* Now we can use the kmem_cache to allocate kmalloc slabs */
 	setup_kmalloc_cache_index_table();
 	create_kmalloc_caches(0);
+#ifdef CONFIG_AMLOGIC_SLAB_TRACE
+	slab_trace_init();
+#endif
 
 	/* Setup random freelists for each cache */
 	init_freelist_randomization();
-- 
2.42.0

